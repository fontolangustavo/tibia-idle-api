spring:
  application:
    name: tibiaidle
  datasource:
    url: jdbc:sqlserver://127.0.0.1:1433;database=tibiaidle;loginTimeout=30;trustServerCertificate=true
    username: sa
    password: Cpc33UBI
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate:
        format_sql: true